#include <iostream>
using namespace std;

typedef struct node {
    int value;
    node* next;
} node;

int main(void) {
    node* head = NULL;
    
    // create node
    node* tmp = (node*)(malloc(sizeof(node)));
    return 0;
}