#include <iostream>
using namespace std;

void swap(int* a, int* b) {
    *a = *a ^ *b;
    *b = *a ^ *b;
    *a = *a ^ *b;
}

void full_permutation(int* ary, int head) {
    for(int i = head, i < )
}

int main(void) {
    int ary[] = {0,1,2,3,4};
    full_permutation(ary, 0, 5x);
}